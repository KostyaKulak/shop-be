# We will install dependencies in our container so we don't need to include them to context
node_modules

# Tests are not running inside container so there is no need to copy it
test

# Is not used for building a container
.elasticbeanstalk
scripts

# ignore files not used
.gitignore
.dockerignore
